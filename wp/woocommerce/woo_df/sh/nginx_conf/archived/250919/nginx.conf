user www www;
worker_processes auto;
error_log /www/wwwlogs/nginx_error.log crit;
pid /www/server/nginx/logs/nginx.pid;
worker_rlimit_nofile 51200;

stream {
    log_format tcp_format '$time_local|$remote_addr|$protocol|$status|$bytes_sent|$bytes_received|$session_time|$upstream_addr|$upstream_bytes_sent|$upstream_bytes_received|$upstream_connect_time';

    access_log /www/wwwlogs/tcp-access.log tcp_format;
    error_log /www/wwwlogs/tcp-error.log;
    include /www/server/panel/vhost/nginx/tcp/*.conf;
}

events {
    use epoll;
    worker_connections 51200;
    multi_accept on;
}

http {
    # 添加自定义日志格式来记录 User-Agent 和 allow_access_ua 值
    log_format debug_format '$remote_addr - $remote_user [$time_local] '
                        '"$request" $status $body_bytes_sent '
                        '"$http_referer" "$http_user_agent" '
                        'allow_access_ua=$allow_access_ua';
    # limit_req_zone $binary_remote_addr zone=perip_req:10m rate=1r/s;
    # 引入cf可信ip配置(建议定期自动调用更新脚本更新)
    include /www/server/nginx/conf/cf-realip.conf;
    # 定义基础限流区域
    # uploads 限流（比图片更通用，包含所有媒体）
    limit_req_zone $binary_remote_addr zone=uploads_limit:10m rate=1r/s;
    # limit_req_zone $binary_remote_addr zone=product_limit:10m rate=1r/s;
    # 产品页访问限流
    limit_req_zone $binary_remote_addr zone=product_limit:10m rate=1r/m;
    # 购物车限流(有效性存疑)
    limit_req_zone $binary_remote_addr zone=cart_limit:10m rate=1r/m;
    # post限流区域定义(暂不引用)
    limit_req_zone $binary_remote_addr zone=post_limit:10m rate=10r/m;

    # 泛性限流(不针对具体链接类型限流)  
    # 所有单个ip的请求限流区域(暂时不引用,可能一起部分模板首页产品卡片添加购物车无法成功)
    limit_req_zone $binary_remote_addr zone=perip_req:10m rate=30r/s;
    # 定义链接数限流区域
    limit_conn_zone $binary_remote_addr zone=perip_conn:10m;
    
    # 定义限流行为(慎用全局限流,容易引起部分模板首页产品卡片添加购物车无法成功)
    # limit_req zone=perip_req burst=100 nodelay;
    # limit_conn perip_conn 2;
    
    # 定义限流返回码
    limit_req_status 429;
    limit_conn_status 429;


    
    # 可选：针对可疑 User-Agent 或空 User-Agent 限流
    map $http_user_agent $allow_access_ua {
        default 0;
        # 允许常见浏览器
        "~*chrome" 1;
        "~*firefox" 1;
        "~*safari" 1;
        "~*edge" 1;
        "~*opera" 1;
        # 暂时允许openai(ai搜索)
        "~*(openai|gptbot)" 1;
        # Google
        # 允许提交收录的google机器人Google-Site-Verification
        "~*Google" 1;
        
        
        # Bing 相关
        "~*bingbot" 1;
        "~*adidxbot" 1;
        "~*msnbot" 1;
        "~*bingpreview" 1;
        "~*microsoft.*protocol.*discovery" 1;
        "~*bingbot-image" 1;

        # 允许 wp定时任务请求
        "~*wordpress" 1;
    }
    map $http_user_agent $bad_bot {
        default 0;
        ~*(AhrefsBot|MJ12bot|SemrushBot|DotBot|BLEXBot|DataForSeoBot|Scrapy|spider|crawler|python-requests|curl|wget) 1;
    }
    # 禁止googlebot访问添加购物车的请求
    map $http_user_agent $is_googlebot {
        ~*Googlebot  1;
        default      0;
    }
    map $is_googlebot$args $block_googlebot_cart {
        ~^1.*add-to-cart  1;
        default           0;
    }

    include mime.types;
    #include luawaf.conf;
    # 宝塔自带片段
    include proxy.conf;
    lua_package_path "/www/server/nginx/lib/lua/?.lua;;";

    default_type application/octet-stream;

    server_names_hash_bucket_size 512;
    client_header_buffer_size 32k;
    large_client_header_buffers 4 32k;
    client_max_body_size 50m;

    sendfile on;
    tcp_nopush on;

    keepalive_timeout 60;

    tcp_nodelay on;

    fastcgi_connect_timeout 300;
    fastcgi_send_timeout 300;
    fastcgi_read_timeout 300;
    fastcgi_buffer_size 64k;
    fastcgi_buffers 4 64k;
    fastcgi_busy_buffers_size 128k;
    fastcgi_temp_file_write_size 256k;
    fastcgi_intercept_errors on;

    gzip on;
    gzip_min_length 1k;
    gzip_buffers 4 16k;
    gzip_http_version 1.1;
    gzip_comp_level 2;
    gzip_types text/plain application/javascript application/x-javascript text/javascript text/css application/xml application/json image/jpeg image/gif image/png font/ttf font/otf image/svg+xml application/xml+rss text/x-js;
    gzip_vary on;
    gzip_proxied expired no-cache no-store private auth;
    gzip_disable "MSIE [1-6]\.";

    limit_conn_zone $binary_remote_addr zone=perip:10m;
    limit_conn_zone $server_name zone=perserver:10m;

    server_tokens off;
    access_log off;

    server {
        listen 888;
        server_name phpmyadmin;
        index index.html index.htm index.php;
        root /www/server/phpmyadmin;

        #error_page   404   /404.html;
        include enable-php.conf;

        location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$ {
            expires 30d;
        }

        location ~ .*\.(js|css)?$ {
            expires 12h;
        }

        location ~ /\. {
            deny all;
        }

        access_log /www/wwwlogs/access.log   main_format  ;
    }
    include /www/server/panel/vhost/nginx/*.conf;
}

